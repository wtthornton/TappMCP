{
  "mcp.servers": {
    "smart-mcp": {
      "command": "node",
      "args": ["dist/server.js"],
      "cwd": "C:\\cursor\\TappMCP",
      "env": {
        "NODE_ENV": "production",
        "HEALTH_PORT": "3001"
      },
      "stdio": true
    },
    "smart-mcp-container": {
      "command": "docker",
      "args": ["exec", "-i", "smart-mcp-prod", "node", "dist/server.js"],
      "env": {
        "NODE_ENV": "production"
      },
      "stdio": true,
      "description": "Connect to manually deployed Docker container (smart-mcp-prod)"
    },
    "smart-mcp-compose": {
      "command": "docker",
      "args": ["exec", "-i", "tappmcp-smart-mcp-1", "node", "dist/server.js"],
      "env": {
        "NODE_ENV": "production"
      },
      "stdio": true,
      "description": "Connect to Docker Compose deployed container (tappmcp-smart-mcp-1)"
    },
    "smart-mcp-npm": {
      "command": "npx",
      "args": ["smart-mcp"],
      "env": {
        "NODE_ENV": "production"
      },
      "stdio": true,
      "description": "Use via NPM package"
    }
  },
  "mcp.enabled": true,
  "mcp.defaultServer": "smart-mcp"
}
